import styled from 'styled-components';

const Language = styled.a.attrs({
	href: '/',
	className: 'language-link'
})`
	font-size: 10px;
	margin-right: 40px;
	text-transform: uppercase;
`;

export default Language;
